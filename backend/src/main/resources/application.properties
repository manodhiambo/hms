# ===========================================
# Hospital Management System - Configuration
# Helvino Technologies Limited
# ===========================================

# Application
spring.application.name=HMS
server.port=8080

# PostgreSQL Database (Neon)
spring.datasource.url=${DATABASE_URL:jdbc:postgresql://ep-dry-cell-ai4bvay3-pooler.c-4.us-east-1.aws.neon.tech/neondb?sslmode=require&prepareThreshold=0}
spring.datasource.username=${DATABASE_USERNAME:REDACTED_DB_USERNAME}
spring.datasource.password=${DATABASE_PASSWORD:REDACTED_ROTATE_THIS_PASSWORD}
spring.datasource.driver-class-name=org.postgresql.Driver
spring.datasource.hikari.maximum-pool-size=5
spring.datasource.hikari.max-lifetime=120000
spring.datasource.hikari.idle-timeout=30000
spring.datasource.hikari.connection-timeout=20000

# JPA / Hibernate
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=false
spring.jpa.properties.hibernate.format_sql=true
spring.jpa.open-in-view=false

# JWT Configuration
jwt.secret=${JWT_SECRET:REDACTED_JWT_SECRET}
jwt.expiration=86400000
jwt.refresh-expiration=604800000

# File Upload
spring.servlet.multipart.max-file-size=10MB
spring.servlet.multipart.max-request-size=10MB

# CORS
app.cors.allowed-origins=${CORS_ORIGINS:http://localhost:5173,http://localhost:3000}

# Mail (for notifications)
spring.mail.host=${MAIL_HOST:smtp.gmail.com}
spring.mail.port=${MAIL_PORT:587}
spring.mail.username=${MAIL_USERNAME:}
spring.mail.password=${MAIL_PASSWORD:}
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true

# Logging
logging.level.com.helvinotech.hms=INFO
logging.level.org.springframework.security=WARN
logging.level.org.springframework.web.servlet.mvc.method.annotation.RequestMappingHandlerMapping=INFO
